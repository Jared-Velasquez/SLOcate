apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: memory-ts-travel
  namespace: chaos-mesh
spec:
  mode: all
  selector:
    namespaces:
      - ts
    labelSelectors:
      app: ts-travel-service
  stressors:
    memory:
      workers: 1
      size: "512MB"
  duration: "3m"
