apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-delay-ts-train
  namespace: chaos-mesh
spec:
  action: delay
  mode: all
  selector:
    namespaces:
      - ts
    labelSelectors:
      app: ts-train-service
  delay:
    latency: "1s"
    jitter: "100ms"
    correlation: "25"
  duration: "3m"
